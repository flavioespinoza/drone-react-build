# drone-react-build
drone.io CI with create-react-app build


```shell
git init
git add README.md
git commit -m "first commit"
git remote add origin https://github.com/flavioespinoza/drone-react-build.git
git push -u origin master
```
